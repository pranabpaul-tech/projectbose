apiVersion : apps/v1
kind: Deployment
metadata:
  name: "projectboseaks-164c"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "projectboseaks-164c"
  template:
    metadata:
      labels:
        app: "projectboseaks-164c"
    spec:
      containers:
        - name: "projectboseaks-164c"
          image: "projectboseacr.azurecr.io/projectboseaks"
          ports:
          - containerPort: 80