apiVersion : apps/v1
kind: Deployment
metadata:
  name: "projectboseaks-9be3"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "projectboseaks-9be3"
  template:
    metadata:
      labels:
        app: "projectboseaks-9be3"
    spec:
      containers:
        - name: "projectboseaks-9be3"
          image: "projectboseacr.azurecr.io/projectboseaks"
          ports:
          - containerPort: 3306