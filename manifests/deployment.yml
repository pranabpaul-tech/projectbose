apiVersion : apps/v1
kind: Deployment
metadata:
  name: "projectboseaks-30eb"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "projectboseaks-30eb"
  template:
    metadata:
      labels:
        app: "projectboseaks-30eb"
    spec:
      containers:
        - name: "projectboseaks-30eb"
          image: "projectboseacr.azurecr.io/projectboseaks"
          ports:
          - containerPort: 80