apiVersion : apps/v1
kind: Deployment
metadata:
  name: "projectboseaks-32d0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "projectboseaks-32d0"
  template:
    metadata:
      labels:
        app: "projectboseaks-32d0"
    spec:
      containers:
        - name: "projectboseaks-32d0"
          image: "projectboseacr.azurecr.io/projectboseaks"
          ports:
          - containerPort: 80