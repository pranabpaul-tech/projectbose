apiVersion : apps/v1
kind: Deployment
metadata:
  name: "projectboseaks-da04"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "projectboseaks-da04"
  template:
    metadata:
      labels:
        app: "projectboseaks-da04"
    spec:
      containers:
        - name: "projectboseaks-da04"
          image: "projectboseacr.azurecr.io/projectboseaks"
          ports:
          - containerPort: 80