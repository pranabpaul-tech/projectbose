apiVersion: v1
kind: Service
metadata:
    name: "projectboseaks-164c"
    labels:
        app: "projectboseaks-164c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "projectboseaks-164c"