apiVersion: v1
kind: Service
metadata:
    name: "projectboseaks-da04"
    labels:
        app: "projectboseaks-da04"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "projectboseaks-da04"