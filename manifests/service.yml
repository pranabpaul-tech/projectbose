apiVersion: v1
kind: Service
metadata:
    name: "projectboseaks-32d0"
    labels:
        app: "projectboseaks-32d0"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "projectboseaks-32d0"