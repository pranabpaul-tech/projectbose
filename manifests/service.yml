apiVersion: v1
kind: Service
metadata:
    name: "projectboseaks-0c97"
    labels:
        app: "projectboseaks-0c97"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "projectboseaks-0c97"