apiVersion: v1
kind: Service
metadata:
    name: "projectboseaks-c4f4"
    labels:
        app: "projectboseaks-c4f4"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "projectboseaks-c4f4"